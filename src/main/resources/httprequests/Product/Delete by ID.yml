info:
  name: Delete by ID
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/products/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
