info:
  name: Delete by ID
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/commands/"
  body:
    type: json
    data: |-
      {
        "customerName": "wesley"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
