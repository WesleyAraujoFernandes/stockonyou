info:
  name: Get All
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/commands"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
