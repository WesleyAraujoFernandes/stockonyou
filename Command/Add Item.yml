info:
  name: Add Item
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/commands/open"
  body:
    type: json
    data: |-
      {
        "commandId": 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
