info:
  name: Get Low Stock
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/products/alerts/low-stock"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
