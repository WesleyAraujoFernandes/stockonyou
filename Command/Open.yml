info:
  name: Open
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/commands/open"
  body:
    type: json
    data: |-
      {
        "customerName": "wesley"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
